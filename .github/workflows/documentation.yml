name: Deploy DocC Documentation

on:
  push:
    branches:
      - main
      - v2.0.0 # Deploy from current branch
  pull_request:
    branches:
      - main
      - v2.0.0
  workflow_dispatch: # Allow manual trigger

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Build Documentation
        run: |
          swift package --allow-writing-to-directory ./docs \
            generate-documentation \
            --target SpecificationKit \
            --output-path ./docs \
            --transform-for-static-hosting \
            --hosting-base-path SpecificationKit

      - name: Add .nojekyll and index.html redirect
        run: |
          touch docs/.nojekyll
          cat > docs/index.html << 'EOF'
          <!DOCTYPE html>
          <html>
          <head>
            <meta charset="utf-8">
            <title>Redirecting to SpecificationKit Documentation</title>
            <meta http-equiv="refresh" content="0; url=./documentation/specificationkit/">
            <link rel="canonical" href="./documentation/specificationkit/">
          </head>
          <body>
            <p>Redirecting to <a href="./documentation/specificationkit/">SpecificationKit Documentation</a>...</p>
            <script>
              window.location.href = "./documentation/specificationkit/";
            </script>
          </body>
          </html>
          EOF

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./docs"

  deploy:
    needs: build
    runs-on: ubuntu-latest

    # Deploy to the github-pages environment
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/v2.0.0'
